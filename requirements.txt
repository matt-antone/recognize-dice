# Core Computer Vision (required)
opencv-python>=4.5.0
numpy>=1.21.0

# GUI and Image Processing
# Note: On Raspberry Pi OS, also run: sudo apt install python3-pil.imagetk
pillow>=8.0.0

# Raspberry Pi Camera (required for Pi)
picamera2>=0.3.0

# TensorFlow Lite - Platform specific installations
# For Raspberry Pi 3: tensorflow-lite-runtime>=2.8.0
# For development on other platforms, use regular tensorflow
# tensorflow>=2.8.0

# GUI (using Tkinter - built into Python, no extra install needed)
# Note: Tkinter is included with Python standard library

# Optional: For older camera modules on Pi
# picamera>=1.13

# Development and utilities
setuptools>=45.0.0

# Optional: For performance monitoring
psutil>=5.8.0 